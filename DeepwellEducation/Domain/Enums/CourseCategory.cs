namespace DeepwellEducation.Domain.Enums;

/// <summary>
/// Top-level course category. The school currently operates as a language centre only (Language).
/// Additional values can be added later without breaking the schema (stored as INTEGER).
/// </summary>
public enum CourseCategory
{
    Language = 0
}
