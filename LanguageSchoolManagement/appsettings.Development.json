{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  
  // SeedAdmin — ONLY FOR DEVELOPMENT (local / Swagger).
  // - Used only when ASPNETCORE_ENVIRONMENT=Development and no Admin user exists.
  // - Production: omit this section; AdminSeeder does not run outside Development.
  // - Do not use these credentials in production; prefer User Secrets for real secrets.
  
  "SeedAdmin": {
    "Email": "admin@local",
    "Password": "Admin123!"
  }
}
