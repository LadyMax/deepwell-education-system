# Copy to ".env" for local tooling, or set variables in your shell before uvicorn.
# INTERNAL_TOKEN must match the C# app setting AiClassifier:InternalToken.

INTERNAL_TOKEN=dev-internal-token

# Future (optional): external model provider keys
# OPENAI_API_KEY=
